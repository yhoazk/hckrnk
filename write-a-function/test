def is_leap(year):
    leap = False

    if ((float(year)%4.0) > 0) or (((float(year)%100.0 > 0) and ((float(year%400)) > 0)):
        leap = True

    return leap


year = int(raw_input())
print is_leap(year)
